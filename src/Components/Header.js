import React from 'react';

function Header () {
    return ( 
        <div>
            <h1>Hangman</h1>
            <h2>Guess the word based on the hint provided</h2>
        </div>
    );
}

export default Header;